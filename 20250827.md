
# 📌 20250828 파일 업로드 방식 수정 - ( getBytes -> transferTo )

---

## A. CommonController 수정 예정 코드

```java
FileUtils.writeByteArrayToFile(newFile, mFile.getBytes()); // 총 2가지 방법으로 수정 가능
```

### ⚠️ 문제점 🚨

- 파일 전체를 **한 번에 `byte[]`로 힙에 올림**
- 그 배열을 파일로 기록 → **메모리 점유 증가**
- 큰 객체가 **GC 대상 → GC 일시중지 시간 증가**

---

## B. 첫 번째 방법: `transferTo` ✨

```java
mFile.transferTo(newFile);
```

- **Spring의 `MultipartFile` 인터페이스**가 제공하는 편의 메서드
- 업로드한 파일(`mFile`)을 **지정한 위치의 파일(`newFile`)로 저장**
- **임시파일 → 목적지 파일로 이동**

---

## C. 두 번째 방법: InputStream / OutputStream 사용 💡

```java
try (InputStream in = mFile.getInputStream();
     OutputStream out = new FileOutputStream(newFile)) {

    byte[] buffer = new byte[8192];
    int bytesRead;

    while ((bytesRead = in.read(buffer)) != -1) {
        out.write(buffer, 0, bytesRead);
    }
}
```

### 설명 :

- `InputStream in = mFile.getInputStream();`
  - 업로드된 파일(mFile)에서 바이트 단위로 읽을 수 있는 입력 스트림을 가져옴 (클라이언트가 업로드한 데이터를 읽는 통로 역할)

- `OutputStream out = new FileOutputStream(newFile)`
  - 서버 쪽에 저장할 대상 파일(newFile)에 출력 스트림을 생성 (서버 디스크에 파일을 쓰는 통로 역할)

- `byte[] buffer = new byte[8192];`
  - 8KB 크기의 바이트 배열 버퍼를 준비 / 파일을 한 번에 다 읽는게 아니라, 조금씩 나눠서 읽고 쓰도록 함

- `while ((bytesRead = in.read(buffer)) != -1)`
  - 입력 스트림에서 buffer 크기만큼 데이터를 읽어옴. 읽은 바이트 수를 bytesRead에 저장. 더 이상 읽을 데이터가 없으면 -1 반환 → 반복 종료.

### 장점 :

- 메모리 사용 최소화 👍
- OOM 방지
- GC 부담 감소
- 대용량 처리 가능

---

## D. 3가지 방식 힙 메모리 상태 변화 📊

- getbytes 방식 (387843.1 -> 421747.5 : 33904.4)

![getbytes](https://github.com/user-attachments/assets/14d1476d-5255-4516-9a04-65153be7e03c)

- inputoutput stream 방식 (331036.0 -> 338255.5 : 7219.5)

![inputoutputstream](https://github.com/user-attachments/assets/77e44834-3fdf-45f5-aca5-c9afbf226346)

- transferto 방식 (484679.8 -> 502896.8 : 18217)

![transferto](https://github.com/user-attachments/assets/8219e3c8-10d4-43a0-9f01-fc9a11e0c582)

- jvm 힙 메모리 상태만 비교 : inputoutputstream > transferto >>> getbytes

---

## E. 적용할 방법 선택과 이유 -> transfer 방식 선택 🏆

- 코드 간단성 : 한 줄이면 끝
- **안정성 : Spring에서 이미 검증한 방식**
- 빠름 : 같은 파일시스템내 단순 저장

---

## F. CommonController.java 수정 부분 🛠️

- `@RequestMapping("/uploadMasterPhotoUpload\")`
- `@RequestMapping("/uploadMasterPhotoUploadFileSizeChk\")`
- `@RequestMapping("/uploadMasterPhotoUpload2\")`
- `@RequestMapping("/uploadMasterChartExcelUpload\")`
```
